openapi: 3.1.0
info:
  title: mealmate API
  version: 0.0.0-stub
  description: |
    mealmate v0.5 是纯客户端 app，**没有自家 API**。
    客户端直连两个外部依赖（Gemini / 自建 YOLO），详 ./api-guide.md。

    v1.1 计划做单个 Cloudflare Worker 中间层，代理 Gemini key + Apple Sign In auth + 同步 mealmate-store。
    OpenAPI 等 v1.1 设计定型后再写。

paths: {}
components: {}
