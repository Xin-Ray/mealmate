# v1.2.5 — 全 stage 取消扣分 (pure encouragement)

**xin 原话**:「不然很伤心的」。

跟 PRD §八 安全与伦理边界 §1 一脉相承(「永远不把 HP 下降描述为『你失败了』」),
v1.2.5 把这条原则推到底:任何 stage 的任何操作都**只奖励,不惩罚**。

## 设计 shift

| 机制 | v1.2.4(老) | v1.2.5(新) |
|---|---|---|
| Stage 1/2/3 漏餐 | `stageScore -10`(clamp 0) | **不动** |
| Stage 4 漏餐 | `HP -10`,HP<0 → demote 回 stage 3 | **HP 不动** |
| Stage 5 某日超体重 | `stars -1`,stars=0 → demote 回 stage 4 | **stars 不动** |
| Stage 5 永久 demote 路径 | 存在 | **无**(stars 只升不降) |
| 漏餐 dialogue 文案 | 「你错过了一餐,有点饿」「贵在坚持」(略带责备/压力) | 「这餐没赶上,下一顿见 🌱」「慢慢来,不急」(中性鼓励) |
| 漏餐 dialogue `hpDelta` | -10 / -5 (gentle) | **0**(UI 不渲染血量 badge) |
| Meal record `hpDelta`(missed) | -10 / -5 | **0** |
| Stage 0/0.5 漏餐 | 已免疫(v1.2.1 起) | 不变,继续免疫 |
| `demoteStage` action | 仍存在,由 addHp(<0)/stage5 触发 | 保留函数,但**无 caller**(dead 但保留备用) |
| `gentleMode` 字段 | 控制漏餐扣分折半 | 保留(字段+ setter),但漏餐路径已不读 |
| WeekStrip missed cell | 红/灰显示 | 灰显示(无变化) |
| 通关条件(stage 1/2/3) | `stageScore >= target` | **不变**(40/50/30) |
| 通关条件(stage 4) | 体重 ≥ standardWeight | **不变** |
| 通关条件(stage 5) | 60 天 + 仍未 demote | **改成纯 60 天**(无 demote 路径) |

## 第二轮 — Mascot 视觉统一正向(build 12 追加)

跟 pure encouragement 一脉相承:**永远不出现疲惫 / 沮丧形象**。
4 band HP 切图机制保留,但 3 个"负面"art 文件被 full.png 内容覆盖:

| File | 用途 | 之前 art | build 12 art |
|---|---|---|---|
| `app/assets/mascot/full.png` | HP 80-100 / Home 默认 | 正面满血 | 不变(源) |
| `app/assets/mascot/stable.png` | HP 50-79 | 中性 | 不变(xin 未点名,中性可保留) |
| `app/assets/mascot/low.png` | HP 30-49 | 残血 | = full.png |
| `app/assets/mascot/critical.png` | HP < 30 | 危险脸 | = full.png |
| `app/assets/mascot/missed.png` | 漏餐 modal/card | 沮丧脸 | = full.png |
| `app/assets/mascot/celebration.png` | 拍餐庆祝 | thumbs-up 男孩 | 不变(本来就正面) |
| `app/assets/mascot/reminder.png` | 餐前提醒 | 中性 | 不变(温和提醒,非负面) |
| `app/assets/mascot/records.png` | 记录页 avatar | 中性 UI 图 | 不变 |

callsites 不动 — `hp.ts` HP_BANDS / `MealIncompleteCard.tsx` / `MissedMealModal.tsx`
仍按 band 切 require,但 4 个路径出来视觉一致。等于把"沮丧脸"从用户视野中删除,
不动机制。

**原则记入 memory**: `feedback_mealmate_mascot_positivity.md`。

## 第三轮 — 错过提醒去图 + 鼓励文案 + 默认关闭(build 13)

xin 追加 directive(跟前两轮一脉相承):「missed 提醒默认关,打开时也只是温和文字,
不要图」。把「missed 通知」从「自动打扰」改成「用户主动开启的温和留档」。

### Store v14 → v15

| 字段 | 类型 | 默认 | 用途 |
|---|---|---|---|
| `missedMealRemindersEnabled` | boolean | **false** | 控制 missedScan 是否 push dialogue + Home 是否显示 MealIncompleteCard |

老 user migrate v14→v15:全部回填 `false`。

### missedScan.ts

- 入口仍跑 `detectMissedSlots` + `markMealMissed`(让 mealRecord status='missed'
  落档,WeekStrip 历史 cell 可显示)
- **但 `missedMealRemindersEnabled === false` 时 skip `pushDialogue`** —— 不污染
  dialogueHistory,records feed 看不到 missed/remind 条
- 文案池重做,15 条 missed + 10 条 remind,全部去「错过/漏/-」字,纯鼓励调

### Home routing(HomeMealStatusSlot)

- `missedRemindersEnabled === false` → 跳过 missed 分支 → 直接 NextMealCard
- `=== true` 时仍走 MealIncompleteCard(text 已重写)

### UI 去图 + 去 badge(4 个文件)

| 文件 | 改动 |
|---|---|
| `RecordCard.tsx` | meal_missed 分支(新):纯文字卡(时间+body) 无 avatar 无 badge。meal kind missed:去 💤 emoji + 「错过了」字 + 红 badge,改「这餐没拍上」中性文 |
| `TodayRecordRow.tsx` | meal_missed + meal missed 都走纯文字 row 无 avatar 无 badge |
| `MealIncompleteCard.tsx` | 删 missed.png Image / 删红字「血量-10」/「我好饿好难受」橘色 → 灰绿,文案改「这餐没拍上,没关系下一顿等你」|
| `MissedMealModal.tsx` | 删 193x173 missed.png / 删「血量-10」badge / 文案统一鼓励调 |

### Settings UI

新 section「通知设置」加在「偏好」之前。Switch 绑 `missedMealRemindersEnabled`,
副标按 xin 原话「打开后,如果错过用餐时间,会在记录里看到一条温和提醒。默认关闭,
因为我们觉得没必要给你压力。」

### 顺手清

「偏好」section 里温柔模式副标 「错过一餐时只扣 5 HP(默认 -10);语气更轻。」
→ 「语气更轻。」(v1.2.5 已删扣分,旧副标 false advertising)

## 改动文件

- `app/src/store/useStore.ts`
  - `markMealMissed`: 删 delta 计算 + 删 stageScore/HP 调用,只落 record(hpDelta=0)
  - `__internal_runStage5Check`: 删「今日超体重 → -1 星 → demote」分支,保留 +1 / 60 天通关
  - 删 `HP_MEAL_MISSED_LOSS` 常量(无 caller)
  - 头部注释更新
- `app/src/services/missedScan.ts`
  - 全套 dialogue 文案改鼓励调
  - `meal_missed` dialogue `hpDelta = 0`
  - 删 `HP_MEAL_MISSED_LOSS` import + `hpLoss` 变量

## 没改的地方

- `demoteStage` 函数本体 — 保留备用(如未来要 reintroduce 加 feature flag 切)。
  实际不会触发:`addHp` 现在只收正 delta(markMealDone +10、addSnack +10),
  `__internal_runStage5Check` 也不再调它。
- `gentleMode` 字段、setter、Settings 开关 — 字段保留,UI 保留。设置仍可切,
  但效果消失(漏餐路径已删 gentleMode 分支)。后续要么删这个 UI、要么给它新含义。
- `RecordCard` / `TodayRecordRow` 血量 badge 渲染 — 守卫已是 `hpDelta < 0`,
  现在 `hpDelta = 0` 自然不渲染,无需改 UI。
- `dialogue.kind === 'failure'` / `pushDialogue(failure)` — 仍存在但不会触发
  (demoteStage 不再被调)。feed render 走暖橘卡的分支保留。

## 与既有原则的关系

| 原则 | v1.2.5 保持 |
|---|---|
| §八.1 永远不把 HP 下降描述为「你失败了」 | ✅ 加强(HP 干脆不下降) |
| §八.6 把"失控"叙述为体验而非缺陷 | ✅ |
| §11.L Stage 1 HP→0 升级 support tone | 路径不会触发,但 modal 保留备用 |
| PRD §11.I 默认值表"错过餐 HP -10" | ⛔ 该行已废,见 PRD `~~strikethrough~~` |
| v1.1 doc Stage 5 -1 星 / demote 表行 | ⛔ 该行已废,见 strikethrough |

## 测试

见 [`testing/v1.2.5-no-penalty.md`](./testing/v1.2.5-no-penalty.md)。

## 风险 / 留账

- **作弊面**: 现在用户即使全天不吃,游戏状态也不退。等于「只能升、不能降」。
  接受 — xin 的 product call: 这是 wellness 工具,不是积分赛。
- **gentleMode 失效**: UI 仍可切但已无效。下一版应该删 Settings 该开关 +
  字段 + setter。本期不删避免污染 hotfix scope。
- **demoteStage 死代码**: 同上,保留备用。
- **failure dialogue kind**: 同上。
- **dev menu `__dev_setHp(0)`**: 在 stage 1/2/3 这个值现在不进通关路径
  (改 stageScore 才是),设了不会触发 demote 因为 addHp(0) 不会让 unclamped<0。
  在 stage 4/5 dev 直接设 hp=0 仍会触发 demote(addHp 路径)? 不会,
  `__dev_setHp` 是 `set({ hp: clampHp(n) })` 直接落,不走 addHp,所以不触发。安全。
