# torch / torchvision must come from the CUDA 12.1 wheel index and be pinned
# to 2.3.x — newer wheels ship cuDNN 9, which fails to initialize on the
# GTX 1080 (compute 6.1). Install before the rest:
#   pip install torch==2.3.1 torchvision==0.18.1 \
#     --index-url https://download.pytorch.org/whl/cu121
# transformers — Food-101 image-classification (HF nateraw/food, ViT-base)
# 首次启动拉 ~350MB 权重到 ~/.cache/huggingface/，部署留好 disk
# 替换 v1.0 的 ultralytics/YOLOv8（COCO 10 食物类太窄，中餐基本全 reject）
transformers>=4.40.0,<4.50.0
# 体重秤 OCR 走 Gemini 2.5 Flash Vision（backend 持有 GEMINI_API_KEY 代理调用），
# 不需要本地 OCR 模型依赖。之前用 easyocr，但 CRNN 对真实 LCD 7 段数字效果差。
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pillow>=10.0.0
pytest>=8.0.0
httpx>=0.27.0
pyjwt[crypto]>=2.8.0
pydantic>=2.0.0
