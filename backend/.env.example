# 复制为 backend/.env（被根 .gitignore 拦下）
# 后端进程读，前端用不到（前端 env 在 app/.env.local）

# Apple Sign-In audience：必须等于客户端 bundle ID（默认 com.xinray.mealmate）
# dev bundle 是 com.xinray.mealmate.dev，需要起一个单独后端实例的话再设这个
MEALMATE_APPLE_AUDIENCE=com.xinray.mealmate

# SQLite 数据库路径。默认 backend/data/mealmate.db
# MEALMATE_DB=/custom/path/mealmate.db

# Gemini API key（体重秤 OCR /ocr/weight 走 Gemini 2.5 Flash Vision）
# https://aistudio.google.com/apikey 拿，写到 backend/.env（gitignored）
# 不能进 git。
GEMINI_API_KEY=
# 可选：换模型。默认 gemini-2.5-flash（够用且便宜）
# GEMINI_MODEL=gemini-2.5-flash
